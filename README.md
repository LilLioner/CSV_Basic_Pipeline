# CSV_Basic_Pipeline
Python and Pandas-based pipeline for processing CSV files, including data cleaning, validation, transformation, and aggregation. Handles missing values and duplicates, with a modular and reusable structure for efficient data analysis workflows.
